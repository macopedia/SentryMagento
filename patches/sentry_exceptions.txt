diff --git a/docroot/app/Mage.php b/docroot/app/Mage.php
index ec30daef7..55ce9d7b8 100755
--- a/docroot/app/Mage.php
+++ b/docroot/app/Mage.php
@@ -847,6 +847,21 @@ final class Mage
 
             $message = addcslashes($message, '<?');
             $loggers[$file]->log($message, $level);
+
+
+            if($file != 'exception.log' && $level <= Zend_Log::ERR) {
+                try {
+                    // send report to sentry!
+                    $DSN = Mage::getStoreConfig('macopedia_sentry/general/app_url');
+                    if(!empty($DSN)) {
+                        $sentryClient = new Macopedia_Sentry_Model_Client($DSN);
+                        $sentryClient->captureMessage($message, array('level' => $level));
+                    }
+                } catch (Exception $s) {
+                        // Ignore errors so that it doesn't crush the website when/if Sentry goes down.
+                }
+            }
+
         }
         catch (Exception $e) {
         }
@@ -863,6 +878,18 @@ final class Mage
             return;
         }
         $file = self::getStoreConfig('dev/log/exception_file');
+
+        try {
+            // send report to sentry!
+            $DSN = Mage::getStoreConfig('macopedia_sentry/general/app_url');
+            if (!empty($DSN)) {
+                    $sentryClient = new Macopedia_Sentry_Model_Client($DSN);
+                    $sentryClient->captureException($e);
+            }
+        } catch (Exception $s) {
+            self::log("\n" . $s->__toString(), Zend_Log::ERR, $file);
+        }
+
         self::log("\n" . $e->__toString(), Zend_Log::ERR, $file);
     }
 
@@ -895,6 +922,17 @@ final class Mage
      */
     public static function printException(Exception $e, $extra = '')
     {
+        try {
+            // send report to sentry!
+            $DSN = Mage::getStoreConfig('macopedia_sentry/general/app_url');
+            if(!empty($DSN)) {
+                    $sentryClient = new Macopedia_Sentry_Model_Client($DSN);
+                    $sentryClient->captureException($e);
+                }
+        } catch (Exception $s) {
+            // Ignore errors so that it doesn't crush the website when/if Sentry goes down.
+        }
+
         if (self::$_isDeveloperMode) {
             print '<pre>';
 
diff --git a/patches/sentry_exceptions.txt b/patches/sentry_exceptions.txt
index 3473bcc96..e69de29bb 100644
--- a/patches/sentry_exceptions.txt
+++ b/patches/sentry_exceptions.txt
@@ -1,64 +0,0 @@
-diff --git a/docroot/app/Mage.php b/docroot/app/Mage.php
-index 3335497f7..309c06605 100755
---- a/docroot/app/Mage.php
-+++ b/docroot/app/Mage.php
-@@ -846,6 +846,21 @@ final class Mage
- 
-             $message = addcslashes($message, '<?');
-             $loggers[$file]->log($message, $level);
-+
-+
-+            if($level <= Zend_Log::ERR && Mage::helper('macopedia_sentry')->reportMessage($message)) {
-+                try {
-+                    // send report to sentry!
-+                    $DSN = Mage::getStoreConfig('macopedia_sentry/general/app_url');
-+                    if(!empty($DSN)) {
-+                        $sentryClient = new Raven_Client($DSN);
-+                        $sentryClient->captureMessage($message, array('level' => $level));
-+                    }
-+                } catch (Exception $s) {
-+                        // Ignore errors so that it doesn't crush the website when/if Sentry goes down.
-+                }
-+            }
-+
-         }
-         catch (Exception $e) {
-         }
-@@ -862,6 +877,20 @@ final class Mage
-             return;
-         }
-         $file = self::getStoreConfig('dev/log/exception_file');
-+
-+        try {
-+            // send report to sentry!
-+            if(Mage::helper('macopedia_sentry')->reportMessage($e->__toString())) {
-+                    $DSN = Mage::getStoreConfig('macopedia_sentry/general/app_url');
-+                    if (!empty($DSN)) {
-+                            $sentryClient = new Raven_Client($DSN);
-+                            $sentryClient->captureException($e);
-+                    }
-+            }
-+        } catch (Exception $s) {
-+            self::log("\n" . $s->__toString(), Zend_Log::ERR, $file);
-+        }
-+
-         self::log("\n" . $e->__toString(), Zend_Log::ERR, $file);
-     }
- 
-@@ -894,6 +923,17 @@ final class Mage
-      */
-     public static function printException(Exception $e, $extra = '')
-     {
-+        try {
-+            // send report to sentry!
-+            $DSN = Mage::getStoreConfig('macopedia_sentry/general/app_url');
-+            if(!empty($DSN)) {
-+                    $sentryClient = new Raven_Client($DSN);
-+                    $sentryClient->captureException($e);
-+                }
-+        } catch (Exception $s) {
-+            // Ignore errors so that it doesn't crush the website when/if Sentry goes down.
-+        }
-+
-         if (self::$_isDeveloperMode) {
-             print '<pre>';
